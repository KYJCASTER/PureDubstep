# Database Configuration
DB_PASSWORD=root

# JWT Configuration (建议修改为更复杂的密钥)
JWT_SECRET=puredubstep-secret-key-for-jwt-token-generation-must-be-at-least-256-bits-long

# Optional: Server Configuration
SERVER_PORT=8080
